{% extends 'base.html.twig' %}

{% block title %}Connexion - {{ parent() }}{% endblock %}

{% block body %}
    <div class="container-quizz" >

    
    {# {{dump(request)}};
    {{dump(qcm)}};
    {{dump(questions)}};
    {{dump(choices)}}; #}

        <h1> Quizz </h1>

        <div class="logoquizz">
            <img src="{{ asset('assets/img/logoQuizz.png')}}" alt="logoquizz">
        </div>
        
       
        
        <form action="" method="POST" id="second" class="section">

            {% for question in qcm.question %}
                {# {{dump(question)}}#}

                <h3>{{question.question}}</h3>

                {% for choice in question.choice %}

                    <div class="container-check" >
                    <input type="checkbox" id="c1" name="cc" value="{{choice.response}}" name="{{question.id}}">
                    <label for=""> <span></span>{{choice.choice}}</label>

                    {% if choice.response == "1" %}

                        {% if request.method == 'POST' %}

                        <div> la bonne reponse est {{choice.choice}}</div>
                
                        {% endif %}

                    {% endif %}

                    </div>

                {% endfor %}

            {% endfor %}


            {% if request.method == 'POST' %}

                {% for answer in request.request %}
                    {% if answer == "1" %}
                        <div>vous avez une bonne reponse</div>
                    {% endif %}
                    {% if answer == "" %}
                        <div>vous avez une mauvaise reponse</div> 
                    {% endif %}
                {% endfor %}

            {% endif %}

            <div class="container text-justify-center">
                <button class='btn btn-primary' onclick="" class='answer'>valider la réponse</button>
            </div>

        </form>                        
    
{% endblock %}