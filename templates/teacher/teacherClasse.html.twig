
{% extends 'base.html.twig' %}

{% block title %}Espace professeur{% endblock %}

{% block body %}
  
  <div class="bg">

  {% include 'teacher/teacherheader.html.twig' %}

      <div class="container-teach">
        
      </div>
</div>
</tr>

  <div class="container">
      {# {{ dump(users)}}; #}
      <table class="table">
        <thead>
          <tr>
            <th scope="col">#</th>
            <th scope="col">Prenom</th>
            <th scope="col">Nom</th>
            <th scope="col">Email</th>
            <th scope="col">Classe</th>
            <th scope="col">Experience</th>
            <th scope="col">Exercices a faire</th>
          </tr>
        </thead>
        <tbody>
        {% for user in users %}
          {% if user.avatar != null %}
          <tr>
            <th scope="row">1</th>
            <td>{{user.firstname}}</td>
            <td>{{user.lastname}}</td>
            <td>{{user.email}}</td>
            <td>6eme1</td>
            <td>{{user.experience}}</td>
            <td>
              <a href="{{ path('student_profil', {id: user.id}) }}"  class="btn btn-primary" id="profil">Profil de l'éléve</a>
             </td>
          </tr>
          {% endif %}
        {% endfor %}
                     
        </tbody>
      </table>

    

  </div>
{% endblock %}
