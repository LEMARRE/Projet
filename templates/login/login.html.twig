{% extends 'base.html.twig' %}

{% block title %}Connexion - {{ parent() }}!{% endblock %}

{% block body %}
{# <div class="overlay trans1"></div> #}
<div class="container-login">

<div class="loginBox">
            
            <div class="logo"></div>
            
            <h3>Identifiez-vous</h3> 
            
            {% if error %}
                <p>Erreur lors de la connexion</p>
            {% endif %}

                <form action="{{ path('login') }}" method="post">
                
                <div class="inputbox">
                    <input type="text" placeholder="Nom d'utilisateur" name="_username" value="{{lastUsername}}"/>
                    <span><i class="fa fa-user"></i></span>
                </div>

                <div class="inputbox">
                    <input type="password" placeholder="Mot de passe" name="_password"/>
                    <span><i class="fa fa-lock"></i></span>
                </div>

                
                <div class="inputbox">
                    <input class="login"type="submit" class="link" value="Login">
                </div>

                <div class="inputbox">
                <p class="para">Vous n'êtes pas inscrit <a href="{{ path('register_student') }}" class="eleve">eleves </a> <a href="{{ path('register_teacher') }}">Prof</a></p>
                </div>

            
            </form>

        </div>
</div>



<div class="container">

    {% if error %}
        <div>Erreur lors de la connexion</div>
    {% endif %}
    

    <div class="row">
        <div class="col-md-3">&nbsp;</div>
        <div class="col-md-6">
            <h2>Connectez-vous !</h2>
            <form action="{{ path('login') }}" method="post">
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="text" name="_username" id="email" placeholder="Email" value="{{ email }}"
                    class="form-control" required>
                </div>
                <div class="form-group">
                    <label for="password">Mot de passe</label>
                    <input type="password" name="_password" id="password" placeholder="Mot de passe"
                    class="form-control" required>
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-success">Connexion</button>
                </div>
            </form>
        </div>
    </div>
</div>

{% endblock %}
